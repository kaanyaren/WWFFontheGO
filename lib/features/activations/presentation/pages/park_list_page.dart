import 'package:flutter/material.dart';

class ParkListPage extends StatelessWidget {
  const ParkListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Park List Page')),
    );
  }
}
